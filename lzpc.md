---
name: lzpc
description: 廉洁风险排查流程分析 — 解析各单位自查表格，生成应然-实然对照、风险评级、现场核验手册等分析报告
---

# lzpc - 廉洁风险排查流程分析工具

## 触发条件

用户输入 `/lzpc <Excel文件路径>` 或 `/lzpc` 后由 Claude 询问文件路径。

## 输入格式

Excel 文件（.xlsx）需包含以下 9 个字段（表头名称模糊匹配，不要求完全一致）：

| # | 字段 | 类型 | 说明 |
|---|------|------|------|
| 1 | 业务名称 | 填空题 | XX业务/工作 |
| 2 | 单位职能 | 单选题 | 业务管理统筹 / 业务实施 / 双重职责 |
| 3 | 排查领域 | 单选题 | 工程建设与抢修维修（含二供）/ 给水工程验收 / 用户报装与接水业务 / 物资及服务采购 / 综合管理 / 其他 |
| 4 | 排查分类 | 多选题 | 按排查领域分组的子类（如项目立项、招投标、验收结算等） |
| 5 | 业务流程 | 填空题 | 自由文本描述，含岗位标注（见举例） |
| 6 | 涉及关键岗位 | 填空题 | XX专责/经理/员 |
| 7 | 廉洁风险点自查描述 | 填空题 | 各环节风险点描述（见举例） |
| 8 | 防控措施清单 | 填空题 | 对应防控措施（见举例） |
| 9 | 备注 | 填空题 | 补充说明 |

## 执行步骤

1. 验证 Excel 文件存在且可读
2. 检查 Python 依赖：`python -c "import openpyxl"`，如未安装则执行 `pip install openpyxl`
3. 运行分析脚本：

```bash
python C:\Users\User\.claude\lzpc\analyzer.py "<输入文件路径>" "<输出目录>"
```

输出目录默认为输入文件同目录下的 `lzpc_output_<时间戳>/`

4. 阅读脚本输出的摘要信息，向用户汇报关键发现：
   - 解析了多少个单位、多少条记录
   - 排查领域-流程推断冲突（建议人工确认的条目）
   - 孤儿业务检测结果（有统筹无实施 / 有实施无统筹）
   - 排查分类与流程节点不匹配的情况
   - 发现多少处红/橙级差异
   - 高风险业务线排名
   - 自查质量最差的 3-5 个单位
   - 疑似抄模板/敷衍填写的单位
   - 需要人工确认的业务名称匹配
5. 提醒用户在输出目录查看完整报告

## 输出文件

| 文件 | 内容 |
|------|------|
| `分析报告.xlsx` | 7 个 Sheet：应然-实然对照矩阵、自查质量排序、高风险靶点清单、业务风险评估、现场核查建议、同业务差异对比、流程完整性分析 |
| `现场核验手册.md` | 可直接打印带往现场的核验手册，按业务线编排，含差异清单、追问问题、查阅资料建议 |

## 核心分析逻辑

### 双源交叉校验
- **排查领域**（字段3）作为主规范名，**流程文本关键字推断**作为独立校验信号
- 两者一致 → 高置信度；矛盾 → 标记"待确认"建议人工核实
- 排查领域为"其他"或空 → 回退到关键字推断

### 孤儿业务检测
- 在同一排查领域内检查统筹与实施的配对完整性
- 有统筹无实施 / 有实施无统筹 → 标记为孤儿业务，摘要中列出

### 排查分类 × 最小节点清单交叉校验
- 单位勾选的排查分类（字段4）与流程描述的节点做对照
- 勾选了分类但流程中无对应节点 → 标记缺漏
- 流程中有但未勾选的节点 → 标记为额外覆盖（仅供参考）

### 自查质量评分（含抄模板检测）
- 四个传统维度：节点完整度、岗位清晰度、岗位标注度、风险自觉度
- 抄模板检测：检测未替换的占位符（如【XX岗位】）和与举例文本的相似度
- 敷衍检测：风险描述和防控措施过短或为套话

### 流程完整性分析（单流程独立审查）
在对比分析之前，先逐条独立审查每条流程的自洽性，不依赖其他单位对照：
- **逻辑合理性**：节点顺序是否合理（审批不出现在执行之后）、是否有重复/高度相似节点、同一岗位是否跨越不相容权力类型（既执行又核验）
- **业务闭环**：是否有明确起点和终点、节点数是否低于该领域最低合理值
- **隐瞒信号**：节点名是否过于笼统（"处理""操作"等）、关键环节是否被不当合并（"采购及验收"）、关键控制节点是否未标注岗位、流程描述是否异常简短

## 维护指南

- **业务名称别名**：编辑 `C:\Users\User\.claude\lzpc\aliases.json`
- **最小节点清单**：编辑 `analyzer.py` 中的 `MIN_NODE_CHECKLIST`
- **排查分类 ↔ 节点对照**：编辑 `analyzer.py` 中的 `INSPECTION_CATEGORY_NODE_MAP`
- **风险评分权重**：编辑 `analyzer.py` 中的 `RISK_WEIGHTS`
- **核查建议模板**：编辑 `analyzer.py` 中的 `VERIFICATION_TEMPLATES`
- **模板占位符/举例片段**：编辑 `analyzer.py` 中的 `TEMPLATE_PLACEHOLDERS` 和 `TEMPLATE_EXAMPLE_SNIPPETS`
- **完整性分析规则**：编辑 `analyzer.py` 中 `PROCESS_INTEGRITY_RULES` 相关常量（`VAGUE_NODE_KEYWORDS` / `CRITICAL_MERGE_PAIRS` / `MIN_PROCESS_NODES` / `CLOSURE_KEYWORDS` / `CRITICAL_POSITION_NODES`）
